# claude-skills

Git-tracked home for skills generated by the `/no` Claude Code skill.

- `skills/<name>/SKILL.md` on `main` = completed (promoted) skills.
- branch `no/<name>` + open PR = in-progress (staged, human-verified, not yet promoted).
- promote = merge the PR to `main` and activate the skill locally.
